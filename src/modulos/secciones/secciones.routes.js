module.exports = (app) => {
  /*
    GET /secciones | obtener todas las secciones?
    GET /secciones/:seccionesId | obtener una secciones
    POST /secciones | crear una secciones
    PUT /secciones/:seccionesId | editar una secciones
    DELETE /secciones/:seccionesId | eliminar una secciones

    PUT /secciones/:seccionesId/preguntas?ids=preguntaId1,preguntaId2 | anadir preguntas a una seccion
  */
}
