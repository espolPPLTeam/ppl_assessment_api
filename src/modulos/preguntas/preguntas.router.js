module.exports = (app) => {
  /*
    GET /preguntas | obtener todas las preguntas?
    GET /preguntas/:preguntasId | obtener una pregunta
    POST /preguntas | crear una pregunta
    PUT /preguntas/:preguntasId | editar una pregunta. Creo que deberia de ser prohibido editar el puntaje de una pregunta
    DELETE /preguntas/:preguntasId | eliminar una pregunta

  */
}
